# دکمه به‌روزرسانی — نصب یک‌باره

بعد از این نصب، در هر فایل اکسل یک **دکمه سبز** بالای شیت اول هست.
هر وقت زدی، داده‌ها از اینترنت گرفته می‌شوند. نه ترمینال، نه پایتون.

---

## گام ۱ — یک تیک در اکسل (فقط یک بار در عمر)

اکسل را باز کن:

```
File → Options → Trust Center → Trust Center Settings
   → Macro Settings
   → ✔ Trust access to the VBA project object model
```

OK بزن و **اکسل را کامل ببند**.

> چرا لازم است: بدون این تیک، هیچ برنامه‌ای اجازه ندارد ماکرو داخل فایل
> اکسل بگذارد — حتی خودت. این محافظ ویندوز است، نه مشکل ما.

## گام ۲ — دوبار کلیک

روی فایل **`Install_Buttons.vbs`** دوبار کلیک کن.

چند ثانیه طول می‌کشد و پیام می‌دهد:

```
۵ فایل آماده شد.
✔ Stocks_Signals.xlsm
✔ Options_Signals.xlsm
✔ Time_Analysis.xlsm
✔ Gold_Analysis.xlsm
✔ FX_Analysis.xlsm
```

## گام ۳ — از این به بعد

فایل‌های **`.xlsm`** را باز کن (نه `.xlsx`).

بار اول اکسل یک نوار زرد نشان می‌دهد → **Enable Content** را بزن.

دکمه سبز **«به‌روزرسانی داده»** بالای شیت اول است. بزن. تمام.

---

## هر دکمه چه می‌کند

| فایل | دکمه | با یک کلیک |
|---|---|---|
| `Stocks_Signals.xlsm` | 🟩 سبز | قیمت، حجم، حقیقی/حقوقی هر ۸ نماد + شاخص کل و هم‌وزن |
| | 🟦 آبی | **تحلیل چرخه زمانی** هر نماد |
| `Time_Analysis.xlsm` | 🟦 آبی | **تحلیل چرخه زمانی** — مستقل، خودش تاریخچه را می‌گیرد |
| `Gold_Analysis.xlsm` | 🟩 سبز | اونس، نقره، سکه تمام/نیم/ربع، گرم ۱۸، دلار + ردیف تاریخچه |
| `FX_Analysis.xlsm` | 🟩 سبز | دلار آزاد، نیمایی، تتر، یورو، درهم + ردیف تاریخچه |
| `Options_Signals.xlsm` | 🟩 سبز | دکمه هست ولی داده آپشن هنوز از این مسیر نمی‌آید |

### دکمه آبی — تحلیل چرخه زمانی

روی شیت `Time_Cycles` است. با یک کلیک، برای هر نماد:

۱. تاریخچه روزانه را از `cdn.tsetmc.com` می‌گیرد
۲. روند خطی را حذف می‌کند (بدون این، خودِ روند در طیف یک چرخه بلندِ جعلی می‌سازد)
۳. طیف دامنه را با **Goertzel** روی شبکه لگاریتمی دوره حساب می‌کند
۴. **پایداری فاز** را با آزمون رایلی می‌سنجد
۵. با **تصحیح Šidák** بابت تعداد فرکانس‌های مستقل، معناداری را قضاوت می‌کند
۶. طول چرخه، کف و سقف اخیر را می‌نویسد

بقیه ستون‌ها (فاز، کف بعدی، عدد گن، پروجکشن فیبوناچی) از قبل فرمول‌اند و
خودشان حساب می‌شوند.

> **«چرخه معناداری پیدا نشد» خرابی نیست.** آشکارساز عمداً سخت‌گیر است.
> روی **۴۰ سری نویز خالص، صفر بار** چرخه دید — و روی ۴۰ سری دارای چرخه
> واقعی، **۱۰۰٪** پیدا کرد، حتی وقتی سیگنال ضعیف و نویز زیاد بود.
> ابزاری که همیشه چرخه می‌بیند، بی‌فایده است.

اگر برای هر ۸ نماد چرخه پیدا کرد، **آن‌وقت** باید مشکوک شوی.

منابع: `tgju.org` (۵ میرور)، `api.nobitex.ir` (۲ میزبان)، `cdn.tsetmc.com`.
همان‌هایی که در `--probe` روی ماشین تو کار کردند.

---

## اگر کار نکرد

| پیام | معنا | کار |
|---|---|---|
| «دسترسی به پروژه VBA بسته است» | گام ۱ انجام نشده | برگرد به گام ۱ |
| «باز نشد (شاید در اکسل باز است)» | فایل باز است | همه فایل‌های اکسل را ببند |
| «هیچ منبعی پاسخ نداد» | اینترنت یا فیلتر | هیچ عددی تغییر نکرده؛ اتصال را چک کن |
| «۳ نماد پاسخ نداد» | بعضی گرفته شد | بقیه نوشته شده؛ دوباره بزن |

**هیچ‌وقت عدد مشکوک نوشته نمی‌شود.** ماکرو قبل از نوشتن، هر عدد را با یک
بازه معقول می‌سنجد (مثلاً دلار بین ۱۰۰ هزار و ۵۰ میلیون ریال). عددی که رد
شود، سلول قبلی‌اش دست‌نخورده می‌ماند.

---

## آنچه دکمه نمی‌کند

**تاریخچه کامل طلا و ارز.** ساخت سری ۵۰۰ روزه و سنجه‌های ارزش‌گذاری
(حباب سکه، پریمیوم تتر) به هم‌تراز کردن چند سری با تاریخ‌های ناهمسان نیاز
دارد که در پایتون بدیهی و در VBA شکننده است.

(تحلیل چرخه زمانی اما **دیگر** به پایتون نیاز ندارد — دکمه آبی خودش
انجامش می‌دهد.)

این‌ها را هر چند وقت یک بار با پایتون بگیر:

```
python scripts/fetch_gold_fx.py --history
python scripts/tse_updater.py --workbook Stocks_Signals.xlsm --history 300
```

⚠️ برای این دستورها **فایل اکسل باید بسته باشد** — پایتون نمی‌تواند فایلی
را که اکسل باز کرده بنویسد.

---

## یک هشدار صادقانه

من ماکرو را از محیط خودم **اجرا نکرده‌ام** — اکسل و ویندوز اینجا نیست.
آنچه آزموده‌ام:

- ساختار هر دو ماژول: توازن بلوک‌ها، اعلام همه متغیرها (Option Explicit)
- منطق تجزیه JSON: روی شکل واقعی پاسخ tgju، نوبیتکس و tsetmc
- تبدیل تاریخ شمسی: روی ۴۰۰۰ روز، صفر اختلاف با موتور پایتون
- نام سلول‌های مقصد: تک‌تک در فایل‌های واقعی بررسی شدند
- **موتور چرخه**: پورت وفادار الگوریتم ماکرو، روی ۱۸ سری (نویز، گشت
  تصادفی، چرخه واقعی) **دقیقاً همان جواب موتور پایتون** را داد
- **ثابت‌های ماکرو** با پیکربندی پایتون تطبیق داده شدند تا دکمه اکسل و
  اسکریپت پایتون دو جواب متفاوت ندهند

۳۹ آزمون ماکرو، همه موفق. ولی «منطق درست است» با «روی اکسل تو اجرا شد» یکی
نیست. اگر خطا داد، **متن کامل خطا** را بفرست.
